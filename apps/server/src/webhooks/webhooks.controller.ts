import { Controller } from '@nestjs/common'

// Placeholder controller - webhooks are managed via platform app
// This controller can be extended in the future for server-side webhook management
@Controller('webhooks')
export class WebhooksController {
  // Webhook CRUD is handled by the platform app
  // This server app only handles webhook delivery
}